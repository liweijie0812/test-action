name: Auto Release

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
jobs:
  defatul:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: liweijie0812/test-action@a
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
  json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: liweijie0812/test-action@a
        with:
          token: ${{ secrets.GITHUB_TOKEN }} 
          version_file: ./test/fixtures/yaml/package.json
  yaml:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: liweijie0812/test-action@a
        with:
          token: ${{ secrets.GITHUB_TOKEN }}     
          version_file: ./test/fixtures/yaml/pubspec.yaml         
